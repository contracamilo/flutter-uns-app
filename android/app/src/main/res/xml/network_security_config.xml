<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security config para desarrollo.

  Android 9 (API 28) por defecto bloquea HTTP sin TLS. Esta config
  habilita cleartext únicamente para los hosts del backend Node.js
  durante desarrollo:

    • 10.0.2.2  → loopback del host visto desde el emulador Android.
    • localhost → emulador en 127.0.0.1 / Wi-Fi reverse-tethering.
    • 192.168.* → IP típica del host en LAN para dispositivos físicos.

  Para producción: borrar esta config y servir HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
